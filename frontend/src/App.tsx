export default function App() {
  return (
    <main className="min-h-screen bg-surface text-ink">
      <section className="mx-auto flex min-h-screen max-w-6xl flex-col justify-center px-6 py-12">
        <p className="text-sm font-medium uppercase tracking-wide text-accent">Cipla EMEU</p>
        <h1 className="mt-3 text-4xl font-semibold">Execution Intelligence Platform</h1>
        <p className="mt-4 max-w-2xl text-base leading-7 text-muted">
          Phase 1 scaffolding is ready. Future phases will add audited ingestion,
          reconciliation, KPI APIs, dashboard pages, and grounded AI summaries.
        </p>
      </section>
    </main>
  );
}
